
<scroll-view scroll-y="true" style="height: 750px; background-color: white" bindscrolltoupper="upper" bindscrolltolower="lower" bindscroll="scroll" scroll-into-view="{{toView}}" scroll-top="{{scrollTop}}">

    <!-- 播放器 -->
     <view class="video-container">
      <video style="width: 100%" src="{{play_url}}" ></video>
    </view> 

    <!-- <view class="video-container">
      <audio poster="{{poster}}" src="{{play_url}}" action="{{audioAction}}" id="myAudio" controls loop></audio>
    </view> -->

    
        <button type="primary" style="margin-left:250px;" bindtap="fileDelete">删除</button>
      

    <!-- 评论区 -->
    <view style="background-color:lightgray; width: 100%; height: 100%">
        
    </view>

</scroll-view>